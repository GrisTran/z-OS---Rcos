export ZOPEN_STABLE_URL="https://gitlab.com/procps-ng/procps/-/archive/master/procps-master.tar.bz2"
export ZOPEN_STABLE_DEPS="zoslib gawk curl gzip coreutils make perl pkgconfig autoconf automake m4"
export ZOPEN_BUILD_LINE="STABLE"


export ZOPEN_EXTRA_CFLAGS=""
export ZOPEN_EXTRA_LDFLAGS=""
export ZOPEN_RUNTIME_DEPS="perl m4"


if [ $LIBPATH = "*/5.24.0/*" ]; then
    export LIBPATH="/home/gristr/zopen/dev/usr/lib:/usr/lpp/IBM/cyp/v3r9/pyz/lib:/lib:/usr/lib:.:/usr/lpp/IBM/zoautil/lib:/usr/lpp/db2d10/jdbc/lib:/usr/lpp/IBM/dbb/lib:/rsusr/ported/lib:/rsusr/ported/lib/perl5/5.38.0/s390/CORE:"
    echo "Updated LIBPATH: $LIBPATH"
fi

zopen_check_results()
{
  dir="$1"
  pfx="$2"
  chk="$1/$2_check.log"

  # Echo the following information to gauge build health
  echo "actualFailures:0"
  echo "totalTests:1"
  echo "expectedFailures:0"
  echo "expectedTotalTests:1"
}

zopen_append_to_env()
{
  # echo envars outside of PATH, MANPATH, LIBPATH
}

zopen_append_to_setup()
{
  # echo commands that will run when installing via setup.sh
}

zopen_get_version()
{
  # Modify to echo the version of your tool/library
  # Rather than hardcoding the version, obtain the version by running the tool/library
  echo "1.0.0"
}
